import createCustomizedElement from '../../utils/createCustomizedElement'
import data from './H1.data.json'

const H1 = createCustomizedElement(data.name, data.attrs, data.children)

export default H1
